<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Crear nuevo proyecto">
    <meta name="author" content="">

    <title>Nuevo Proyecto · EXAMEN 1</title>
    <link rel="shortcut icon" href="resources/css/logoFICCT.jpg" type="image/x-icon">

    <!-- Fuentes y estilos -->
    <link href="resources/admin/vendor/fontawesome-free/css/all.min.css" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Nunito:400,600,700" rel="stylesheet">
    <link rel="stylesheet" href="resources/css/create_project.css">

    <style>
        /* Pequeños ajustes complementarios para mejorar layout sin tocar la paleta */
        .main-container {
            max-width: 900px;
            margin: 48px auto;
            padding: 0 16px;
        }

        .form-card {
            display: flex;
            gap: 32px;
            align-items: center;
        }

        .form-card .illustration {
            flex: 1;
            text-align: center;
        }

        .form-card .form-area {
            flex: 1;
        }

        @media (max-width:720px) {
            .form-card {
                flex-direction: column
            }
        }

        /* Header personalizado con paleta */
        .main-navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: linear-gradient(180deg, #E6EADF 0%, #D6DAC8 100%);
            padding: 12px 20px;
            border-radius: 8px;
            box-shadow: 0 6px 18px rgba(0, 0, 0, 0.06);
            margin: 18px auto;
            max-width: 980px;
        }

        .main-navbar .navbar-brand {
            font-weight: 700;
            color: #556B55;
            font-size: 20px;
        }

        .main-navbar .navbar-user {
            display: flex;
            gap: 12px;
            align-items: center;
            color: #556B55;
        }

        .main-navbar .user-name {
            font-weight: 600;
        }

        .main-navbar .logout-link {
            color: #D06B6B;
            text-decoration: underline;
        }

        /* Modal styling to match palette */
        .modal-content {
            border-radius: 10px;
            overflow: hidden;
        }

        .modal-header {
            background: #f3f6f1;
            border-bottom: 1px solid rgba(0, 0, 0, 0.06);
        }

        .modal-title {
            color: #35603F;
            font-weight: 700;
        }

        .modal-body {
            color: #445a45;
            padding: 20px;
        }

        .modal-footer {
            background: #fafaf6;
            border-top: 1px solid rgba(0, 0, 0, 0.04);
        }

        .modal-footer .btn {
            min-width: 110px;
        }

        .modal-footer .btn-cancel {
            margin-right: 8px;
        }

        /* Ensure footer stays visually balanced */
        footer.footer {
            max-width: 980px;
            margin: 20px auto;
        }
    </style>

</head>

<body>

    <main class="main-container" role="main">
        <section class="create-project-form form-card" aria-labelledby="create-title">
            <div class="illustration">
                <img src="resources/css/architecture-icon.png" alt="Icono proyecto"
                    style="max-width:220px; opacity:0.95;">
                <p style="color:#6B7B6A; margin-top:12px;">Crea un proyecto para comenzar a modelar tu diagrama UML.</p>
            </div>

            <div class="form-area">
                <h1 id="create-title" style="margin-top:0;">NUEVO PROYECTO | 📌</h1>
                <form action="/store" method="POST" aria-describedby="project-help">
                    <div class="form-group">
                        <label for="name">Nombre del proyecto</label>
                        <input id="name" name="name" type="text" required tabindex="1" autofocus
                            placeholder="Ej: BibliotecaSistema" />
                        <small id="project-help" style="display:block; margin-top:8px; color:#556B55;">Usa un nombre
                            corto y único.</small>
                    </div>

                    <div class="form-buttons" style="margin-top:18px;">
                        <a href="/proyecto" class="btn-cancel" role="button">Cancelar</a>
                        <input type="submit" class="btn-save" value="Guardar">
                    </div>
                </form>
            </div>
        </section>
    </main>

   

    <!-- Scripts al final para mejorar carga -->
    <script src="resources/admin/vendor/jquery/jquery.min.js"></script>
    <script src="resources/admin/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script src="resources/admin/vendor/jquery-easing/jquery.easing.min.js"></script>
    <script src="resources/admin/js/sb-admin-2.min.js"></script>
    <script src="resources/admin/vendor/chart.js/Chart.min.js"></script>
    <script src="resources/admin/js/demo/chart-area-demo.js"></script>

    <!-- CDNs auxiliares -->
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>

    <% if (typeof alert !=="undefined" ) { %>
        <!-- Datos serializados como JSON en un script para evitar inyección directa en JS -->
        <script id="swal-data" type="application/json"><%- JSON.stringify({
                title: alertTitle || '',
                text: alertMessage || '',
                icon: alertIcon || 'info',
                showConfirmButton: !!showConfirmButton,
                timer: (typeof timer !== 'undefined' && !isNaN(Number(timer))) ? Number(timer) : 1500,
                ruta: ruta || ''
            }) %></script>

        <script>
            (function () {
                try {
                    const raw = document.getElementById('swal-data').textContent;
                    const _swalData = JSON.parse(raw);
                    Swal.fire(_swalData).then(() => { window.location = '/proyecto' + (_swalData.ruta || ''); });
                } catch (e) {
                    console.error('Error parsing swal data:', e);
                }
            })();
        </script>
        <% } %>

</body>

</html>